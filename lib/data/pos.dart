class Pos {
  int pos_id;
  String tgl_transaksi;
  int items;
  double total_harga;

  Pos(
  {required this.pos_id,
  required this.tgl_transaksi,
  required this.items,
  required this.total_harga});
}
